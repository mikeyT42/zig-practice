const std = @import("std");

const Sums = struct {
    positive: f32,
    negatice: f32,
    overall: f32,
};

const Counts = struct {
    positive: u16,
    negative: u16,
    overall: u16,
};

const Averages = struct {
    positive: f32,
    negative: f32,
    overall: f32,
};

pub fn main() !void {}
